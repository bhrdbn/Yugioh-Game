package controller;

import java.util.regex.Matcher;

public class LoginController {
/*
    Player player = new Player();
    public void createUser(String userName, String nickName, String password) {
        player.setUsername(userName);
        player.setNickname(nickName);
        player.setPassword(password);
    }


    public boolean checkUserName(String username){
        for (Player player: player.getPlayer) {
            if(player.getUserName().eqauls(username))
                return false;
        }
        return true;
    }


    public boolean checkNickName(String nickName){
        for (Player player: player.getPlayer) {
            if(player.getNickName().eqauls(nickName))
                return false;
        }
        return true;
    }


    public boolean checkPassword(String password){
        for (Player player: player.getPlayer) {
            if(player.getPassword().eqauls(password))
                return true;
        }
        return false;
    }

    public void showScoreboard() {
        ScoreBoard scoreBoard = new ScoreBoard();
        scoreBoard.printScoreBoard();
    }

    public String logoutUser(Matcher matcher3) {
        return null;
    }

    public String loginUser(Matcher matcher2) {
        return null;
    }*/
}
